# Step 1: core API skeleton only. Each later step appends what it needs
# (SQLAlchemy/Alembic in step 2, auth in step 3, Groq/pgvector/sentence-transformers
# in steps 9-10) so Docker rebuilds stay fast and every dependency's purpose is traceable
# to the step that introduced it.
fastapi==0.118.0
uvicorn[standard]==0.37.0
pydantic==2.11.7
pydantic-settings==2.7.1

# Step 2: database layer
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Step 3: auth
bcrypt==4.2.1
pyjwt==2.10.1
email-validator==2.2.0
redis==5.2.1

# Step 9: RAG (local embeddings + pgvector)
# CPU-only torch build — the default index pulls a multi-GB CUDA wheel that's
# wasted weight in a container with no GPU.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1
sentence-transformers==3.3.1
pgvector==0.3.6

# Step 10: AI coach — OpenRouter and Ollama both speak the OpenAI-compatible
# chat-completions/tool-calling API, so one client library covers both.
openai==1.59.6

# Step 12: tests
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1

# Step 13: Google Sign-In — verifies Firebase ID tokens against Google's
# public keys (google-auth), no Firebase Admin SDK / service account needed.
# cachecontrol wraps the HTTP session so those signing certs are cached
# instead of re-fetched on every single sign-in (see app/services/firebase_auth.py).
google-auth==2.36.0
requests==2.34.2
cachecontrol==0.14.1

# Avatar upload — python-multipart is FastAPI's required parser for
# UploadFile/File(...) form bodies.
python-multipart==0.0.20
